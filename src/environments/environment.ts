import { EnvironmentConfig } from '@core/environment.service';

export const environment: EnvironmentConfig = {
  production: false,
  backendUrl: 'http://localhost:5078',
  tokenAudience: 'https://kimlyknet.dev.local',
};
